<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- #306 Phase G — Workspaces rail package. -->
    <Description>Bowire Workspaces rail — workspace switcher (closest thing to a file tree in Bowire). Adds the Workspaces rail descriptor to the rail registry.</Description>
    <PackageId>Kuestenlogik.Bowire.Rail.Workspaces</PackageId>
    <PackageTags>bowire;rail;workspaces;workbench;plugin</PackageTags>
    <IsPackable>true</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Kuestenlogik.Bowire\Kuestenlogik.Bowire.csproj" />
  </ItemGroup>
</Project>
