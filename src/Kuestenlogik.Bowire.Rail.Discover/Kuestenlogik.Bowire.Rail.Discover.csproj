<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!--
      #306 Phase G — Discover rail package. Schema-driven services tree
      (the "what can I call?" surface). Ships only the descriptor; the
      JS still lives in core wwwroot/js until the Phase G continuation
      lands.
    -->
    <Description>Bowire Discover rail — schema-driven services tree. Adds the Discover rail descriptor to the rail registry.</Description>
    <PackageId>Kuestenlogik.Bowire.Rail.Discover</PackageId>
    <PackageTags>bowire;rail;discover;workbench;plugin</PackageTags>
    <IsPackable>true</IsPackable>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Kuestenlogik.Bowire\Kuestenlogik.Bowire.csproj" />
  </ItemGroup>
</Project>
