---
uid: Kuestenlogik
title: Kuestenlogik namespace
summary: 'Root namespace stub — all Bowire APIs live under Kuestenlogik.Bowire.*. Lets the auto-generated breadcrumb resolve cleanly.'
---

# `Kuestenlogik` namespace

The root namespace acts purely as a parent — every public Bowire API surface lives under [Kuestenlogik.Bowire](Kuestenlogik.Bowire.html) and its sub-namespaces. DocFX doesn't auto-generate a page for a namespace that holds only other namespaces (no direct types), so this stub plugs the gap that the breadcrumb on every type page links into.

## Next

- [Kuestenlogik.Bowire](Kuestenlogik.Bowire.html) — the host SDK entry point: `IBowireProtocol`, `BowireOptions`, the `MapBowire()` extension.
- [API reference index](index.html) — full namespace + assembly map across the core + first-party protocol plugins.
