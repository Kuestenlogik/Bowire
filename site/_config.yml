title: Bowire
description: Multi-protocol API workbench for .NET. Auto-discovers gRPC / REST / GraphQL / MQTT / SignalR / WebSocket / SSE / MCP / OData services and gives you a single browser UI to inspect, invoke, stream, mock, and replay them — embedded in your app or as a standalone CLI.
url: "https://bowire.io"
baseurl: ""

# Used by jekyll-seo-tag to emit og:image / twitter:card image, and by
# the Bowire brand wrapper everywhere else (about page, footer, etc.).
image: /assets/images/og-image.png
twitter:
  card: summary_large_image
markdown: kramdown
kramdown:
  syntax_highlighter: rouge
theme: null
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
